.. _proxy-configuration:

Caching Proxy Configuration
===========================

You need to configure the caching proxy of your choice. These guides help you
for the configuration for the features of this library. You will still need to
know about the other features of the caching proxy to get everything right.

.. toctree::

    varnish-configuration
    nginx-configuration
    symfony-cache-configuration
